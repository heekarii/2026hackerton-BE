"""공용 FastAPI 의존성 모음.

NOTE: 인증(학교 이메일 인증/로그인)은 별도 브랜치에서 구현 예정입니다.
      현재는 임시 스텁으로 고정 user_id 를 반환합니다. 실제 인증이 머지되면
      get_current_user_id 의 본문만 실제 인증 로직으로 교체하면 되고,
      이를 사용하는 엔드포인트 시그니처는 변경할 필요가 없습니다.
"""


def get_current_user_id() -> int:
    """현재 로그인한 사용자의 id 를 반환한다.

    TODO(auth): 실제 인증 의존성으로 교체. 임시로 1번 사용자를 작성자로 사용한다.
    """
    return 1
